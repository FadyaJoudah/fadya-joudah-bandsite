let comments = [{}, {}, {}]; // default comment

function insertComment(comment) {
  const commentSecrion = document.find; // comment section
  commentSecrion.add``;
}

for (let index = 0; index < comments.length; index++) {
  insertComment(comments[index]);
}
